dist: trusty
language: node_js
node_js:
  - '12.14.1'

branches:
  only:
    - development

addons:
  chrome: stable

before_install:
  sh build.sh

script:
  cd frontend && npm run test-single

notifications:
  email: false

cache:
  directories:
    - backend/node_modules
    - frontend/node_modules

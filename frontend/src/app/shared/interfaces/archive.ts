import { ArchiveYear } from './admin';

export interface HarpString {
  color: string;
  xPosition?: number;
  trembling?: boolean;
}

export interface ArchiveYearBlock {
  archiveYear: ArchiveYear;
}
